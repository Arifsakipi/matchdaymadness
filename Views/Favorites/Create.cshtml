@model MatchdayMadness2.Models.Favorites

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Favorites</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group">
                <label asp-for="Teamsid" class="control-label"></label>
                <select asp-for="Teamsid" class="form-control" asp-items="ViewBag.Teams"></select>
                <span asp-validation-for="Teamsid" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="PlayersID" class="control-label"></label>
                <select asp-for="PlayersID" class="form-control" asp-items="ViewBag.Players"></select>
                <span asp-validation-for="PlayersID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
